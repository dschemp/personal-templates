## Ubuntu 20.04 Server Minimizer

This playbooks will setup an Ubuntu Server 20.04 instance to its bare minimum needed to run.
The goal is to create a minimal installation of Ubuntu without some hassle of the minimum image (like no UEFI support).
